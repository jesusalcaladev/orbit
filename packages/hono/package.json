{
  "name": "@orbit/hono",
  "version": "0.0.1",
  "description": "Hono middleware for @orbit/core — mount the full Orbit protocol (JSON, MessagePack, SSE streaming, uploads) on any Hono app.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "keywords": [
    "orbit",
    "hono",
    "middleware",
    "data-layer",
    "protocol"
  ],
  "license": "MIT",
  "dependencies": {
    "@orbit/core": "workspace:*",
    "hono": "^4.2.0"
  },
  "devDependencies": {
    "@hono/node-server": "^1.13.0",
    "@types/node": "^24.0.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
