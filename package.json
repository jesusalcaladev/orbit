{
  "name": "orbit",
  "version": "0.0.1",
  "description": "Modular data layer for the modern fullstack — monorepo root. The protocol core lives in packages/core (@orbit/core).",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "husky",
    "build": "pnpm -r run build",
    "typecheck": "pnpm run build && pnpm -r run typecheck && tsc -p tsconfig.json",
    "test": "pnpm -r run test",
    "test:watch": "pnpm --filter @orbit/core run test:watch",
    "test:coverage": "pnpm --filter @orbit/core run test:coverage",
    "format": "biome check --write .",
    "lint": "biome check .",
    "example": "pnpm run build && node examples/node/standalone-server.ts",
    "examples": "pnpm run build && node examples/node/run-all.ts",
    "web": "pnpm run build && node examples/web/server.ts",
    "bench": "pnpm run build && node bench/run.ts",
    "size": "pnpm run build && node bench/size.ts"
  },
  "lint-staged": {
    "*.{ts,js,json}": [
      "biome check --write"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@hono/node-server": "^1.13.0",
    "@orbit/core": "workspace:*",
    "@orbit/express": "workspace:*",
    "@orbit/hono": "workspace:*",
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "dataloader": "^2.2.3",
    "express": "^4.18.2",
    "graphql": "^17.0.2",
    "graphql-ws": "^6.2.1",
    "hono": "^4.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "typescript": "^7.0.2",
    "ws": "^8.21.3"
  }
}
